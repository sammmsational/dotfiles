local colors = require 'colors'

-- Equivalent to the --bar domain
sbar.bar {
  topmost = 'window',
  height = 43,
  color = colors.dark_grey,
  padding_right = 15,
  padding_left = 15,
  blur_radius = 10,
  notch_width = 220,
}
